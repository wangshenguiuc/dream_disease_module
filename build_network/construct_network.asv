function network = construct_network(QA,file_head)

[nnode,ndim] = size(QA);

D = pdist(X,'cosine');

for thres = 0.9:-0.1:0.1
    D_sub = D.*(D>thres);
    [i,j,v] = find(D_sub);
    file_name = ['../Data/Network/embed_network/',file_head,num2str(thres)];
    fid=fopen(file_name,'wt');
    nedge = length(i);
    for k=1:nedge
        fprintf(fid, '%s\t%s\t%f\n', valu) ;
    end
    fclose(fid);
end

end

